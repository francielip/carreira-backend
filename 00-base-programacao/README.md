# 📂 Módulo 00 - Base de Programação

Este diretório contém os fundamentos da minha jornada. É aqui que aprendi a "falar" a lógica que o computador entende.

## 🧠 1. Pensamento Computacional e Lógica
Antes de digitar, é preciso planejar.
- **Decomposição:** Dividir um problema grande (ex: fazer um bolo) em passos pequenos (pegar ovos, bater massa).
- **Algoritmo:** A sequência exata desses passos. Se a ordem estiver errada, o código "quebra".

## 🐍 2. Python: Minha Primeira Aplicação
Aprendi como o computador recebe e exibe dados:
- `input()`: Abre os ouvidos do computador para o usuário falar.
- `print()`: O computador "fala" de volta na tela.
- **Tipos de Dados:**
  - `str`: Textos ("Olá")
  - `int`: Números inteiros (10)
  - `float`: Números decimais (10.5)

## ⚖️ 3. Condicionais (Tomada de Decisão)
Onde o código escolhe caminhos usando o `if`, `elif` e `else`.
- **Operadores de Comparação:** `==` (igual), `!=` (diferente), `>` (maior), etc.
- **Dica de Ouro:** A indentação (o espaço no começo da linha) é o que diz ao Python o que está "dentro" do `if`. Sem ela, o código não roda!



## 🔄 4. Laços de Repetição (Loops)
Para não precisar repetir o mesmo código mil vezes:
- `for`: Uso quando sei exatamente quantas vezes quero repetir (ex: percorrer uma lista de alunos).
- `while`: Uso quando quero que algo repita **enquanto** uma condição for verdadeira (ex: até o usuário digitar "sair").

## 📦 5. Funções (Reutilização)
Para deixar o código limpo e organizado.
- `def minha_funcao(parametro):`: Crio um bloco de código que posso "chamar" a qualquer momento pelo nome.
- `return`: O resultado que a função devolve para quem a chamou.

---
> "Programar não é decorar comandos, é saber como conectar essas ferramentas para resolver um problema."
